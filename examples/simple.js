import spec from 'spectrallogs'
import specweb from 'spectrallogs/web'
let number = 3;
let bool = (Math.random() < 0.5);

number = 2;
/* inmut number */

let array = []
function Main(x = 3) {
    spec.log("types " + x)
}

let newer = 4;
Main(newer)
